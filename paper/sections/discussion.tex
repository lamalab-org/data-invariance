\section{Discussion}
\label{sec:discussion}

\paragraph{Reducing cross-sample prediction churn.}
The intro's toxicity classifier loses class on
$\churnMin\text{--}\churnMax\%$ of test molecules between retrainings
on independent bootstraps of the same training population.
Bagging-$K{=}5$ cuts that to
$\bagFiveFinalLow\text{--}\bagFiveFinalHigh\%$ at no accuracy cost
($5\times$-ERM compute, Table~\ref{tab:main}).  Twin-bootstrap at
$\lambda{=}300$ ($2\times$-ERM compute) cuts it to
$\twinFinalLow\text{--}\twinFinalHigh\%$,
beating matched-compute bagging-$K{=}2$ on
$\twinWinsBagTwoMain/\twinWinsBagTwoMainDenom$ datasets and
matching the $K{=}5$ result in mean rank
(Appendix~\ref{app:friedman}).

\paragraph{A recommended workflow.}
A tiered recipe that practitioners can adopt incrementally on an
existing pipeline.  (i)~Bagging-$K{=}5$ on the deployed predictor,
parallelisable to ERM wall-clock at $5\times$ compute
(Table~\ref{tab:main}).  (ii)~Twin-bootstrap at the rule-selected
$\lambda$ when $5\times$ compute is prohibitive; recovers most of the
$K{=}5$ benefit at $2\times$-ERM compute (Figure~\ref{fig:main}).
(iii)~For triage on top of any deployed model, train one extra ERM
bootstrap and route the top-$\reviewFraction\%$-churn predictions
to a reviewer (Figure~\ref{fig:decile}).

\paragraph{Stability is a property of the procedure.}
A model's cross-sample churn is not a fact about the trained
weights; it is a fact about the procedure that produced them ---
the optimiser, the schedule, the data-resampling protocol.  In the
codistillation-vs.\ twin-bootstrap comparison
(Appendix~\ref{app:overlap_spectrum}), a single hyperparameter on
the procedure --- the fraction of data the two networks share ---
separates a setting where churn increases on DILI ($\overlapPctHi\%$
overlap) from one where churn never increases on any dataset
($\sim\overlapPctMid\%$).  The same lever, the consistency
weight $\lambda$, takes a different value on a different architecture
but the rule that selects it is unchanged
(Section~\ref{sec:generalisation}).

\paragraph{Cross-sample churn belongs in the benchmark report.}
AI-for-science benchmarks (MoleculeNet, TDC ADME/Tox,
materials-science) report columns for accuracy, AUROC, and
calibration.  None varies the training dataset; cross-sample churn does, and on every benchmark we
test the per-example flip rate is many times larger than the
aggregate-accuracy drift it lives behind
(Section~\ref{sec:churn-magnitudes}, Figure~\ref{fig:overview}).
Clinical biostatistics already reports per-individual stability
intervals~\citep{Riley2023}; the case for doing the same in
scientific ML rests on the magnitudes we measure.  The protocol
surfaces the rate, bagging and twin-bootstrap reduce it, and a
one-bootstrap triage identifies which predictions will flip first.
