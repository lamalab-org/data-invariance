\section{Cross-sample churn flips $\churnMin$--$\churnMax\%$ of test predictions on \nDatasetsHeadline{} chemistry benchmarks}
\label{sec:churn-magnitudes}

Table~\ref{tab:fragility-magnitudes} reports the cross-bootstrap
class-flip rate and the corresponding distributional
gap (sym-KL) for each of the \nDatasetsHeadline{} chemistry datasets
that pass the $+\preregFilterPP$pp ERM-vs-majority filter, alongside training-set
size $N$, ERM accuracy, and the mean pairwise accuracy difference
$|\Delta\text{acc}|$ between two retrainings.  Class-flip rate ranges from $\churnMin\%$ (BBB-Martins)
to $\churnMax\%$ (MOF-thermal).  Aggregate accuracy moves
$\accDiffMin\text{--}\accDiffMax$\,pp between two retrainings while
individual predictions disagree at
$\churnAccRatioMin\text{--}\churnAccRatioMax\times$ that rate.
Sym-KL varies by ${\sim}\symKLDatasetSpread\times$ across datasets,
and the variation is task-dependent rather than a function of
training-set size alone (e.g.\ TADF and BACE both have
$N \approx 1000$ but differ by ${\sim}2\times$ in sym-KL).
Per-example churn exceeds aggregate-metric drift on every dataset
under any aggregate (precision, recall, $F_1$, AP;
Appendix~\ref{app:additional_metrics}) and concentrates on the
minority class on imbalanced datasets (BBB-Martins, BBBP, CYP2D6-Sub;
Appendix~\ref{app:per_class_churn}) --- the class practitioners
typically act on.

\input{sections/tables/fragility_magnitudes.tex}

The within-task $N$-scaling on BACE (Appendix~\ref{app:nscaling})
trends downward with a log-log slope of $\nScalingSlope$ and reaches a
soft floor near $M{\approx}\nScalingMmin$ before plateauing --- churn
decreases with training-pool size but not to zero.
