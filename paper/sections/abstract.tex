\begin{abstract}
Two classifiers trained on independent samples of the same chemistry
training population assign different classes to $8\text{--}17\%$ of
test molecules with no measurable change in aggregate accuracy.  We
call this \emph{cross-sample prediction fragility}: the population-level
analogue of \emph{prediction churn}~\citep{TODO_milani2016}.  Where
deployment-loop work measures churn between a new model and an
incumbent on web-scale data, we measure it between two equally
weighted bootstraps of the training pool, in the small-$N$ from-scratch
regime that scientific machine learning inhabits and where no incumbent
exists.  On eight chemistry benchmarks (MoleculeNet, TDC ADME/Tox,
materials science) fragility exceeds the parameter-variance axis
captured by deep ensembles by an order of magnitude.  $K$-bootstrap
bagging cuts argmax churn $47\text{--}49\%$ with no accuracy
regression.  A sym-KL consistency loss between two networks trained
on independent bootstraps (\emph{twin-indep}) cuts it $69\text{--}72\%$
at matched compute against bagging-$K{=}2$, and reduces distributional
disagreement by an additional order of magnitude.  Twin-indep is the
codistillation family of \citet{TODO_anil2018_codistillation} at
$\sim$$40\%$ inter-network bootstrap overlap; on a 10-dataset
comparison it is the only operating point on that family that does
not catastrophically fail.  The recipe applies in the small-$N$
from-scratch regime; on pretrained-backbone settings the consistency
loss over-regularises features the backbone already encodes.
\end{abstract}
