# TADF emission wavelength — real chemistry dataset.
# Binary classification: short-wavelength (< 492 nm) vs long-wavelength (>= 492 nm).
# No spurious-correlation injection: this paper measures cross-sample
# fragility under the natural training distribution.  (The injection
# mechanism remains in TADFDataset for hypothetical use elsewhere.)
name: tadf
arch: mlp
parquet_path: data/chemistry/tadf_preprocess.parquet
data_dir: ./data
