# Multi-spurious CMNIST — two independent spurious features (colour + brightness).
# Tests whether the method handles overlapping shortcuts that JTT cannot distinguish.
name: multi_cmnist
arch: mlp
color_correlation: 0.9
brightness_correlation: 0.8
test_color_correlation: 0.1
test_brightness_correlation: 0.2
label_noise: 0.25
data_dir: ./data
